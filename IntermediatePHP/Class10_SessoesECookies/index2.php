
<?php
session_start();

/*
// exemplo 1
// o if (isset($_SESSION['name'])){} verifica se a variável de sessão 'name' existe
if (isset($_SESSION['name'])) {
    echo 'Olá, ' . $_SESSION['name'];
} else {
    echo "Olá, visitante";
}

// o unset($_SESSION['name']); remove a variável de sessão 'name'
// o session_destroy(); remove todas as variáveis de sessão
 */
